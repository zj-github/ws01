package cn.innohub.crawler.firstlevel.seeds.extract;

import java.util.Comparator;

import cn.innohub.crawler.beans.FirstLevelSeed;

public class SeedComparator implements Comparator<FirstLevelSeed> {

	@Override
	public int compare(FirstLevelSeed o1, FirstLevelSeed o2) {
		return o1.getNextFetchTime().getTime() - o2.getNextFetchTime().getTime()>0?1:-1;
	}

}